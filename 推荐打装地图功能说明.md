# 推荐打装地图功能说明

## 功能概述

这个功能根据玩家的职业和等级，智能推荐最适合打装备的地图。系统会分析：

- 玩家当前等级（±10级范围内的装备）
- 玩家职业（只推荐适合该职业的装备）
- 装备掉落概率和怪物数量（计算综合权重）

## 实现架构

### 1. ItemInformationProvider 增强

**位置**: `gms-server/src/main/java/org/gms/server/ItemInformationProvider.java`

**新增功能**:

- `equipByJobAndLevelCache`: 装备按职业和等级分组的缓存（每5级一组）
- `initEquipCache()`: 初始化装备缓存，排除cash装备
- `getEquipsByJobAndLevel(Job, minLevel, maxLevel)`: 根据职业和等级范围获取装备列表
- `getDropInfoByItems(List<itemId>)`: 批量查询装备的掉落信息（怪物ID和掉率）

### 2. MapFactory 增强

**位置**: `gms-server/src/main/java/org/gms/server/maps/MapFactory.java`

**新增功能**:

- `monsterMapCache`: 怪物-地图-数量的三层缓存映射
- 在 `loadLifeRaw()` 中自动记录怪物出现的地图和数量
- `getMapsByMonsterId(monsterId)`: 查询单个怪物出现的所有地图
- `getMapsByMonsterIds(List<monsterId>)`: 批量查询多个怪物的地图分布

### 3. AbstractPlayerInteraction 组合查询

**位置**: `gms-server/src/main/java/org/gms/scripting/AbstractPlayerInteraction.java`

**新增功能**:

- `getRecommendedEquipMaps()`: 获取推荐地图列表（按权重排序）
  - 权重计算公式: `Σ(装备掉率 × 怪物数量)`
  - 返回包含地图ID、权重、怪物信息的列表
- `getMapEquipDetail(mapId)`: 获取指定地图的详细信息
  - 返回该地图中所有掉落适合装备的怪物详情

**内部类**:

- `MapRecommendation`: 地图推荐信息（mapId, weight, monsters）
- `MonsterEquipInfo`: 怪物装备信息（monsterId, count, equips）
- `EquipDropInfo`: 装备掉落信息（itemId, chance）
- `MapDetailInfo`: 地图详细信息（mapId, monsters）

### 4. JavaScript 客户端脚本

**位置**: `gms-server/scripts-zh-CN/BeiDouSpecial/掉宝地图.js`

**交互流程**:

1. **主界面** (`levelmain`): 显示推荐地图列表
   - 显示地图名称、ID、权重、掉落怪物种类数
   - 按权重降序排列
   - 最多显示前20个地图

2. **详情界面** (`levelshowMapDetail`): 显示地图详情
   - 地图基本信息
   - 怪物列表（名称、等级、血量、数量）
   - 每个怪物掉落的装备列表
   - 装备的基础掉率和玩家实际掉率

## 使用方法

### 服务器端配置

1. 确保数据库中存在 `drop_data` 表（记录装备掉落信息）
2. 服务器启动后，装备缓存会在首次调用时自动初始化
3. 怪物-地图缓存在地图加载时自动构建

### 玩家使用

玩家可以通过以下方式打开脚本：

- NPC对话触发
- 命令触发（如果配置了命令系统）
- 按钮/菜单触发

示例NPC脚本：

```javascript
// npc/9000000.js (示例)
function start() {
  cm.openNpc(9000000, "掉宝地图");
}
```

## 技术特点

1. **智能缓存**:
   - 装备信息按职业和等级分组缓存，避免重复查询
   - 怪物-地图关系在地图加载时自动缓存
   - 使用ConcurrentHashMap保证线程安全

2. **精确筛选**:
   - 根据reqJob位标志正确解析装备的职业要求
   - 考虑玩家等级±10级范围
   - 自动排除cash装备

3. **权重算法**:
   - 综合考虑装备掉率和怪物数量
   - 按权重排序，优先推荐综合收益最高的地图

4. **用户友好**:
   - 清晰的界面展示
   - 显示玩家实际掉率（考虑掉率加成）
   - 支持返回上级菜单

## 扩展建议

1. **增加过滤选项**:
   - 按装备类型过滤（武器、防具等）
   - 按怪物等级过滤
   - 按地图区域过滤

2. **优化算法**:
   - 考虑装备品质（蓝装、紫装）
   - 考虑地图距离和传送费用
   - 考虑怪物难度（攻防血量）

3. **数据统计**:
   - 记录玩家使用情况
   - 统计热门打装地图
   - 提供个性化推荐

## 注意事项

1. 首次使用时装备缓存需要初始化，可能需要几秒钟
2. 怪物-地图缓存依赖地图加载，未加载的地图不会出现在推荐列表中
3. 如果找不到适合的地图，可能是等级过低或职业特殊
4. 权重计算基于理论掉率，实际掉落还受随机因素影响

## 版本信息

- **版本**: 1.0
- **日期**: 2025-01-27
- **作者**: AI Assistant
- **兼容性**: BeiDou-Server (基于OdinMS)

## 更新日志

### v1.0 (2025-01-27)

- 初始版本发布
- 实现基础的装备推荐功能
- 支持按职业和等级筛选
- 实现权重排序算法

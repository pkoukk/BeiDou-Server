# 推荐打装地图功能 - 快速开始

## 一、功能简介

这是一个智能的装备获取辅助系统，能够根据玩家的职业和等级，自动推荐最适合打装备的地图。

### 核心特性

- ✅ 按职业智能筛选装备
- ✅ 考虑玩家等级范围（±10级）
- ✅ 综合计算掉落权重（掉率×怪物数量）
- ✅ 实时显示玩家实际掉率
- ✅ 详细展示怪物信息
- ✅ 自动排除cash装备

## 二、快速部署

### 步骤1：代码已就绪

以下文件已经修改完成：

1. ✅ `ItemInformationProvider.java` - 装备缓存和查询
2. ✅ `MapFactory.java` - 怪物地图缓存
3. ✅ `AbstractPlayerInteraction.java` - 组合查询接口
4. ✅ `掉宝地图.js` - 客户端交互脚本

### 步骤2：配置NPC（可选）

如果想通过NPC调用，可以使用提供的示例：

```javascript
// 复制 scripts-zh-CN/npc/打装推荐NPC示例.js
// 重命名为你想要的NPC ID，例如：9000000.js

function start() {
  cm.openNpc(cm.getNpc(), "掉宝地图");
  cm.dispose();
}
```

### 步骤3：重启服务器

重启服务器以加载新代码：

```bash
cd gms-server
./launch.sh  # Linux/Mac
# 或
launch.bat   # Windows
```

### 步骤4：测试功能

1. 进入游戏
2. 与配置的NPC对话，或直接调用脚本
3. 查看推荐的打装地图列表

## 三、使用方法

### 方法一：通过NPC

```javascript
// 在任意NPC脚本中添加选项
function start() {
  var text = "需要帮助吗？\r\n";
  text += "#L1#推荐打装地图#l\r\n";
  cm.sendSimple(text);
}

function action(mode, type, selection) {
  if (selection == 1) {
    cm.openNpc(cm.getNpc(), "掉宝地图");
    cm.dispose();
  }
}
```

### 方法二：通过命令（需要命令系统）

如果服务器支持命令系统，可以添加一个命令：

```java
// 示例：@装备推荐
public class EquipMapCommand extends Command {
    @Override
    public void execute(Client c, String[] params) {
        NPCScriptManager.getInstance().start(c, 0, "掉宝地图", null);
    }
}
```

### 方法三：直接调用

在其他脚本中直接调用：

```javascript
// 例如在event脚本中
cm.openNpc(9000000, "掉宝地图");
```

## 四、界面预览

### 主界面示例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【推荐打装地图】
职业：战士  等级：50
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
以下地图按综合掉落权重排序：

🗺 废弃都市  [ ID: 103000000 ]
    权重：5.23  |  掉落怪物种类：8 种

🗺 蚂蚁洞穴  [ ID: 105040300 ]
    权重：4.87  |  掉落怪物种类：5 种

...
```

### 详情界面示例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【废弃都市】
地图ID：103000000
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

掉落装备的怪物列表：

👹 红蜗牛  [ Lv.5 | HP: 250 ]
    地图数量：3 只
    装备掉落：
      🗡 新手剑
        基础掉率: 0.5%  |  你的掉率: 1.0%
      🛡 木盾
        基础掉率: 0.3%  |  你的掉率: 0.6%
```

## 五、配置选项

### 修改等级范围

在 `AbstractPlayerInteraction.java` 中修改：

```java
// 默认是 ±10 级
int minLevel = Math.max(1, level - 10);
int maxLevel = level + 10;

// 可以改为 ±15 级
int minLevel = Math.max(1, level - 15);
int maxLevel = level + 15;
```

### 修改显示数量

在 `掉宝地图.js` 中修改：

```javascript
// 默认显示20个地图
var displayCount = Math.min(20, recommendedMaps.size());

// 可以改为30个
var displayCount = Math.min(30, recommendedMaps.size());
```

### 修改权重算法

在 `AbstractPlayerInteraction.java` 中修改权重计算：

```java
// 当前算法：掉率 * 怪物数量
weight += (chance / 1000000.0) * monsterCount;

// 可以改为其他算法，例如加入怪物等级因素
int monsterLevel = LifeFactory.getMonsterLevel(monsterId);
double levelFactor = 1.0 + (monsterLevel - level) * 0.1;
weight += (chance / 1000000.0) * monsterCount * levelFactor;
```

## 六、故障排除

### 问题1：显示"没有找到适合的地图"

**原因**：

- 玩家等级太低（1-10级）
- 职业特殊（GM等）
- 地图未加载

**解决**：

- 提升玩家等级
- 加载更多地图（传送到各个地图）

### 问题2：装备列表为空

**原因**：

- 装备缓存未初始化
- 数据库drop_data表数据缺失

**解决**：

- 等待缓存初始化（首次加载需要几秒）
- 检查数据库表是否有数据

### 问题3：地图列表不全

**原因**：

- 怪物-地图缓存依赖地图加载
- 只有已加载的地图才会被统计

**解决**：

- 让管理员预加载所有地图
- 或者在服务器启动时加载关键地图

### 问题4：权重计算不准确

**原因**：

- 掉率是理论值
- 实际掉落受随机因素影响

**解决**：

- 这是正常现象
- 可以调整权重算法增加准确性

## 七、性能优化建议

### 1. 缓存预热

在服务器启动时预加载缓存：

```java
// 在服务器启动代码中添加
ItemInformationProvider.getInstance().getEquipsByJobAndLevel(Job.WARRIOR, 1, 200);
```

### 2. 定期清理

如果内存紧张，可以定期清理缓存：

```java
// 添加定时任务清理旧缓存
equipByJobAndLevelCache.clear();
equipCacheInitialized = false;
```

### 3. 数据库优化

为drop_data表添加索引：

```sql
CREATE INDEX idx_drop_itemid ON drop_data(itemid);
CREATE INDEX idx_drop_dropperid ON drop_data(dropperid);
```

## 八、进阶开发

### 扩展1：添加装备类型过滤

```java
// 在getRecommendedEquipMaps中添加参数
public List<MapRecommendation> getRecommendedEquipMaps(int equipType) {
    // equipType: 1=武器, 2=防具, 3=饰品
    // 筛选逻辑...
}
```

### 扩展2：添加地图难度评估

```java
// 计算地图难度（怪物平均等级、血量等）
public double calculateMapDifficulty(int mapId) {
    // 难度计算逻辑...
}
```

### 扩展3：添加个性化推荐

```java
// 根据玩家历史数据推荐
public List<MapRecommendation> getPersonalizedMaps() {
    // 读取玩家打怪记录
    // 分析偏好
    // 返回个性化推荐
}
```

## 九、技术支持

如有问题或建议，请：

1. 查看详细文档：`推荐打装地图功能说明.md`
2. 检查服务器日志
3. 联系开发者

## 十、许可证

本功能基于 BeiDou-Server (OdinMS) 开发
遵循 GNU Affero General Public License v3.0

---

**版本**: 1.0  
**更新日期**: 2025-01-27  
**作者**: AI Assistant based on user requirements
